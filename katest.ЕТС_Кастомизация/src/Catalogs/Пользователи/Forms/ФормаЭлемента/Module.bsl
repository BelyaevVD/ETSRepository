

&НаСервере
Процедура ЕТС_ПередЗаписьюНаСервереПосле(Отказ, ТекущийОбъект, ПараметрыЗаписи)
	
	Если Не ЗначениеЗаполнено(Текущийобъект.ЕТС_Код) Тогда
		Текущийобъект.ЕТС_Код = ЭтаФорма.ПользовательИБИмя;
	КонецЕсли;
КонецПроцедуры

