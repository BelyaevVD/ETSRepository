
&НаКлиенте
Процедура ЕТС_СкладПриИзмененииПосле(Элемент)
	Объект.Грузоотправитель = ЕТС_ОбщиеКомандыСервер.ПолучитьГрузоотправителя(Склад);
КонецПроцедуры

&НаКлиенте
Процедура ЕТС_ПодразделениеПриИзмененииПосле(Элемент)
	Если ЗначениеЗаполнено(Объект.Организация) И ЗначениеЗаполнено(Объект.Подразделение) Тогда
		ЗаполнитьЗначенияСвойств(Объект,ЕТС_ОбщиеКомандыСервер.ПолучитьОтветственноеЛицо(Объект.Организация,Объект.Подразделение));
	КонецЕсли;
КонецПроцедуры
