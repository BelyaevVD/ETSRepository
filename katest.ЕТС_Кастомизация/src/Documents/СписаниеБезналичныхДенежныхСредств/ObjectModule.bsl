
&Перед("ОбработкаПроведения")
Процедура ЕТС_ОбработкаПроведения(Отказ, РежимПроведения)
	Если РольДоступна("ЕТС_ОсобыйРежимПроведения") Тогда
		ЕТС_ИзменениеРежимаПроведения.УстановитьПривелигированный(Истина);
	КонецЕсли;
КонецПроцедуры

&После("ОбработкаПроведения")
Процедура ЕТС_ОбработкаПроведения1(Отказ, РежимПроведения)
	Если РольДоступна("ЕТС_ОсобыйРежимПроведения") Тогда
		ЕТС_ИзменениеРежимаПроведения.УстановитьПривелигированный(Ложь);
	КонецЕсли;
КонецПроцедуры
