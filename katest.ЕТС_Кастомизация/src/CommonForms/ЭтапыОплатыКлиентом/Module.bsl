
&НаСервере
&После("ЗаполнитьЭтапыОплаты")
Процедура ЕТС_ЗаполнитьЭтапыОплаты(ЗаполнятьФормуОплаты)
	Если ЗначениеЗаполнено(Договор) Тогда
		ОтсрочкаПлатежа = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Договор,"ЕТС_ОтсрочкаПлатежа");
		Для каждого Стр Из ЭтапыГрафикаОплаты Цикл
			Стр.ДатаПлатежа = Стр.ДатаПлатежа + 86400 * ОтсрочкаПлатежа;
		КонецЦикла;
	КонецЕсли;
КонецПроцедуры
