Функция ЭтоРабочаяБаза() Экспорт 

	//СтрокаСоединенияИБ = "test";
	СтрокаСоединенияИБ = СтрокаСоединенияИнформационнойБазы(); 
	//Если СтрНайти(СтрокаСоединенияИнформационнойБазы(), СтрокаСоединенияИБ,) = 0 Тогда
	Если СтрНайти(СтрокаСоединенияИБ, """ka""",) = 0 И СтрНайти(СтрокаСоединенияИБ, """ka_test_olga""",) = 0 Тогда
		Возврат Ложь;     
	Иначе
		Возврат Истина;
	КонецЕсли;

КонецФункции // ЭтоРабочаяБаза()
