
&После("ПередЗаписьюНаСервере")
// тут пишем проверки обязательных реквизитов перед записью
// Подразделение - заполнение обязательно
Процедура ЕТС_ПередЗаписьюНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи, Форма)
	Если Не ЗначениеЗаполнено(ТекущийОбъект.Подразделение) Тогда
		ОбщегоНазначенияКлиентСервер.СообщитьПользователю(
		НСтр("ru='Укажите подразделение!'"),
		ТекущийОбъект,
		,,
		Отказ);
	КонецЕсли;
КонецПроцедуры
